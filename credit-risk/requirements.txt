numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
matplotlib>=3.8
tabulate>=0.9

# Optional: fetches the dataset straight from Kaggle. Without it (or without
# Kaggle credentials) the pipeline falls back to the calibrated stand-in.
kagglehub>=0.3
